run `docker build -t evaluation-function .` and `docker run --rm -v "D:\Desktop\study\notes\Imperial\SEGP\FSA-eval:/app" -w /app evaluation-function` from the root

in Dockerfile.eval replace the boilerplate folder to FSA-eval for switching the builds

good luck and f**K SEGP
